import 'package:flutter/material.dart';

class ColorsManager {
  ColorsManager._();
  static const Color mainBlue = Color(0xFF007AFF);
  static const Color primary = Color(0xff08431D);
  static const Color white = Color(0xffffffff);
  static const Color moreGray = Color.fromRGBO(234, 235, 237, 1);
}
